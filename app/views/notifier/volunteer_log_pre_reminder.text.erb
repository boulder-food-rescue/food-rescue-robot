Hi <%= @volunteer.name %>,

Just a quick reminder that you have <%= @logs.length == 1 ? "an" : "" %> upcoming shift<%= @logs.length > 1 ? "s" : "" %>:

<% @logs.each{ |l| %>
 - <%= l.when.strftime("%A") %>: <%= l.donor.name %> to <%= l.recipient.name %> (<%= l.food_type.nil? ? "Produce/Baked Goods" : l.food_type.name %>)
<% } %>

Thanks!

*chirp* *beep*

--
The Robot
