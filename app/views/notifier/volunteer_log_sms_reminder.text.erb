Need data for: <%= @logs.collect{ |l| "#{l.when}: #{l.donor.nil? ? "?" : l.donor.name}->#{l.donor.nil? ? "?" : l.donor.name} (#{l.food_types.length == 0 ? "Miscellany" : l.food_types.collect{ |ft| ft.name }.join(",") }), http://alpha.boulderfoodrescue.org/logs/#{l.id}/edit" }.join(" :: ") %>
