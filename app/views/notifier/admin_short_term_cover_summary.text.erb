These shifts are coming up and need to be covered...

<% @logs.each{ |l| %>
  <%= l.when.strftime("%A") %>: <%= l.donor.nil? ? "?" : l.donor.name %> -> <%= l.recipient.nil? ? "?" : l.recipient.name %> <%= "(" + l.food_type.name + ")" unless l.food_type.nil? %>
<% } %> 

Thanks!

*beep* *beep*

--
The Robot
