Hi <%= @volunteer.name %>,

This is a friendly reminder from the food robot that you need to enter
some data for your recent pickup<%= @logs.length > 1 ? "s" : "" %>:

<% @logs.each{ |l| %>
<%= l.when %>: <%= l.donor.nil? ? "?" : l.donor.name %> to <%= l.recipient.nil? ? "?" : l.recipient.name %> (<%= l.food_types.length == 0 ? "Miscellany" : l.food_types.collect{ |ft| ft.name }.join(",") %>)
Link: <%=url_for :controller=>'logs', :action=>'edit', :id=>l.id, :only_path=>false  %>
This is your <%= l.num_reminders.ordinalize %> reminder.

<% } %>
Thanks!

--
The Food Robot
