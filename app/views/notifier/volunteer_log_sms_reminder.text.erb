Need data for: <%= @logs.collect{ |l| "#{l.when}: Delivery to #{l.recipients.collect{ |r| r.nil? ? nil : r.name }.compact.join(", ")} #{l.food_types.length == 0 ? "Miscellany": l.food_types.collect{ |ft| ft.name }.join(",")} (#{l.food_types.length == 0 ? "Miscellany" : l.food_types.collect{ |ft| ft.name }.join(",") }), #{url_for :controller=>'logs', :action=>'edit', :id=>l.id, :only_path=>false}" }.join(" :: ") %>
