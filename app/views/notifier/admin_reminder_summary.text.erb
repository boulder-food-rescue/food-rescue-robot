These volunteers need to enter their data:

<% @logs.each{ |l| %>
  <%= l.volunteers.blank? ? "?" : l.volunteers.collect{ |v| v.name }.join(" and ") %>, <% l.donors.each do |donor| %> <%= donor.name %> <% unless donor == l.donors.last %>, <% end end %> -> <%= %> on <%= l.when %> (<%= l.num_reminders %> reminders sent)
<% } %> 
