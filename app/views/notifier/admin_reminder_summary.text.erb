These volunteers need to enter their data:

<% @logs.each{ |l| %>
  <%= l.volunteers.blank? ? "?" : l.volunteers.collect{ |v| v.name }.join(" and ") %>, <%= l.donor.nil? ? "?" : l.donor.name %> -> <%= l.recipient.nil? ? "?" : l.recipient.name %> on <%= l.when %> (<%= l.num_reminders %> reminders sent)
<% } %> 
