Need data for: <%= @logs.collect{ |l| "#{l.when}: #{l.donor.nil? ? "?" : l.donor.name}->#{l.recipient.nil? ? "?" : l.recipient.name} (#{l.food_type.nil? ? "Produce/Baked Goods" : l.food_type.name}), http://alpha.boulderfoodrescue.org/logs/#{l.id}/edit" }.join(" :: ") %>
