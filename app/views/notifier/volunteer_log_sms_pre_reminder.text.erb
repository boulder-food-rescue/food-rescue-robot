Upcoming pickups: <%= @logs.collect{ |l|  "#{l.when.strftime("%A")}: #{l.donor.name} to #{l.recipient.name} #{l.food_types.length == 0 ? "Miscellany": l.food_types.collect{ |ft| ft.name }.join(",")}" }.join(" :: ") %>
